# Orchestration Log — Lisa Simpson

**Timestamp:** 2026-03-05T21:46:29Z  
**Agent:** Lisa Simpson (Core Dev / Extension Developer)  
**Task:** Project Scaffold — VS Code Extension Structure  
**Status:** SUCCESS

## Overview

Full VS Code extension project structure scaffolded with dual build pipeline (esbuild for extension host, Vite for webview).

## Files Produced

### Extension Host
- `package.json` — Root project manifest with esbuild, TypeScript, ESLint, npm-run-all
- `tsconfig.json` — Extension host config (Node16, ES2022 target)
- `esbuild.js` — Build script with watch mode, asset copy plugin
- `eslint.config.mjs` — Flat config (ES2024, TypeScript support)
- `.gitignore` — Updated with dist/, node_modules/, etc.

### VS Code Config
- `.vscodeignore` — Package exclusions (node_modules, sources, tests)
- `.vscode/launch.json` — Extension debugger (F5 to debug, Ctrl+K Ctrl+E to reload)
- `.vscode/tasks.json` — npm watch, build, check-types tasks
- `.vscode/settings.json` — TypeScript, ESLint, formatting
- `.vscode/extensions.json` — Recommended extensions (ESLint, TypeScript)

### Webview (React 19 + Vite)
- `webview-ui/package.json` — Vite, React, Canvas 2D, messaging libs
- `webview-ui/tsconfig.json` — Solution-style with references
- `webview-ui/tsconfig.app.json` — App code (ESNext, JSX, bundler module)
- `webview-ui/tsconfig.node.json` — Build tools (ESNext, no DOM)
- `webview-ui/vite.config.ts` — Output to dist/webview/assets/, fixed names
- `webview-ui/index.html` — Single-page app root
- `webview-ui/src/main.tsx` — Vite entry point
- `webview-ui/src/App.tsx` — Canvas placeholder component
- `webview-ui/src/vscodeApi.ts` — VS Code webview API wrapper (handles acquireVsCodeApi lifecycle)
- `src/.gitkeep` — Placeholder for source files

## Architecture Decisions Embedded

1. **Dual Build Pipeline:** esbuild (Node.js/CJS) for extension, Vite (React/ESM) for webview
2. **TypeScript:** Root tsconfig for extension, solution-style in webview-ui
3. **Asset Handling:** esbuild plugin copies webview-ui/public/assets → dist/assets
4. **VS Code Config:** Panel webview, instant activation (empty activationEvents)
5. **Dev Workflow:** `npm run watch` for parallel esbuild + tsc, `npm run check-types` for type checking only

## Context for Other Agents

- **Bart (Canvas Dev):** Webview scaffold ready at webview-ui/src/ — App.tsx, vscodeApi.ts, main.tsx
- **Marge (Tests):** Extension tests consume dist/extension.js; webview tests use Vite test mode
- **Ned (Docs):** Build pipeline documented in esbuild.js comments; setup = `npm install` → `npm run build`
- **Homer (Coordinator):** Two builds, both via simple npm scripts; watch mode for fast iteration

## Next Steps

1. Scribe logs this orchestration, merges decision to decisions.md
2. Bart starts frontend work with webview scaffold in place
3. Extension host code (src/extension.ts) created as needed by other agents
