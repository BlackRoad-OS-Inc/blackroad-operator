name: Deploy to Octavia

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Octavia
        run: |
          echo "Deploy via SSH to Octavia :3100-3103"
          echo "TODO: scp docker-compose.yml + services/ to octavia"
          echo "TODO: ssh blackroad@octavia 'cd roadcode-platform && docker compose up -d --build'"
