#!/bin/bash
# br-launch — branded animation launcher
W='\033[1;37m'; D='\033[0;90m'; N='\033[0m'
NAME="$1"; SCRIPT="$2"
printf "\n  ${W}BLACKROAD OS${N}  ${D}%s${N}\n" "$NAME"
printf "  ${D}────────────────────────────────────────────────${N}\n\n"
bash "$SCRIPT"
