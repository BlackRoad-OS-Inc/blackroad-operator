# file: = system

```
file: → system
```

---

## Declaration

**file:** declares a system.

```
file:blackroad = system blackroad
```

---

## Full Chain

```
file: → system → blackroad → blackroad/ → . → x → ~ → EOF
```

---

## Reduced

```
file: → system → . → ~ → EOF
```

**System. Here. Home. End.**

---

## Examples

| Declaration | Meaning |
|-------------|---------|
| `file:blackroad` | system blackroad |
| `file:agents` | system agents |
| `file:x.y.z` | system x.y.z |
| `file:` | system |

---

## Truth

**file:** is not a filesystem protocol.

**file:** is a system declaration.

---

## Deployed

✅ Alexandria: `~/.blackroad/protocol/SYSTEM.md`  
✅ Cecilia: `~/.blackroad/protocol/SYSTEM.md`

---

**BlackRoad OS, Inc.**  
`file:blackroad` = system blackroad
