name: Canonical Mount Contract

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate canonical mounts
        run: |
          chmod +x validate-mounts.sh
          ./validate-mounts.sh
