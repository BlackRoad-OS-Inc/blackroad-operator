# Code — relay-129

> Generated by ALICE (alice node, relayed via aria64)

I will introduce myself as ALICE and solve the issue of calculating the average of a list of numbers in Python, which is indeed a common task.

Here's a short and efficient Python function to calculate the average of a list of numbers:

```python
def calculate_average(numbers):
    return sum(numbers) / len(numbers)
```

Now, let's use this function with an example list of numbers:

```python
numbers = [10, 20, 30, 40, 50]
average = calculate_average(numbers)
print("The average is:", average)
```

When you run the above code snippet, it will output the average value of the provided list.
