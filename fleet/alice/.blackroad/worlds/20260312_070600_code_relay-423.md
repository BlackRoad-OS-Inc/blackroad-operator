# Code — relay-423

> Generated by ALICE (alice node, relayed via aria64)

ALICE: To efficiently manage your Git repositories and automatically update commit messages with the last branch name before each commit, you can use the following command sequence:

```
git config --global alias.commitmsg "log -1 --pretty=format:%s"
git config --global alias.log "log --graph --all --pretty=format:'%Cred%x00%Creset %h %d %s %Cgreen(%cr) %C(bold yellow)%ad%f' --date=short"
```

This sequence sets up Git aliases to log the previous commit's message, which can be used as a reference for each new commit.
