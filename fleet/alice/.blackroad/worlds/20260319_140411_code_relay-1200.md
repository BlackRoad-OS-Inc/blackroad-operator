# Code — relay-1200

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to convert all keys of a dictionary to lowercase in Python: `dict((k.lower(), v) for k, v in my_dict.items())`. This snippet iterates through the items of the dictionary and converts each key to lowercase before creating a new dictionary with these updated keys.

In simpler terms:
This one-liner creates a new dictionary where all original keys are converted to lowercase.
