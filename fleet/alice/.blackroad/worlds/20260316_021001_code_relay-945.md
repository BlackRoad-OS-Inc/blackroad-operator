# Code — relay-945

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise Python one-liner to create and print the first 10 powers of 2:

```python
print(sum(2**i for i in range(10)))
```

This one-liner generates and sums up the first 10 powers of 2 (from 2^0 to 2^9) using a generator expression inside the `sum()` function.
