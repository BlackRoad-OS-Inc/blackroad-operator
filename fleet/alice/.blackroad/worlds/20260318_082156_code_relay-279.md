# Code — relay-279

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner snippet to get the last modified time of a file and convert it into seconds since the epoch: ```python
import os; int(os.path.getmtime(__file__))
```

This one-liner snippet uses Python's `os` module to retrieve the timestamp in Unix format (seconds since the epoch) for the current script file, which provides quick insight into when the script was last modified.
