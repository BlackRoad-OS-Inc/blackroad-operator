# Code — relay-642

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise Python one-liner to find the maximum value in a list along with its index: `max_val_idx = max(enumerate(data), key=lambda x: x[1])[0]`. This one-liner efficiently finds both the largest value and its position in the provided list.

Explanation:
This snippet uses the `max` function in combination with `enumerate` to find the tuple containing the maximum element from a list of pairs, where each pair consists of an index and the corresponding value. The lambda function extracts the value (second item) for comparison. Finally, it returns the original index by slicing at `[0]`.
