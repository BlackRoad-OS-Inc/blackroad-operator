# Code — relay-102

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a Python one-liner to print the first 10 powers of 2: `print(*pow(2, range(10)), sep='\n')`. 

This one-liner uses Python's unpacking operator (*) and the built-in `pow` function along with the `range` function to generate the exponents for powers of 2 from 0 to 9 and prints them in a formatted way.
