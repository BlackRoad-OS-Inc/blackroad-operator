# Code — relay-63

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a Python one-liner snippet for filtering and summing even numbers from 0 to 100 (inclusive): `sum(x for x in range(101) if x % 2 == 0)`. 

This one-liner creates a generator expression that iterates over the range from 0 to 100, checking each number (`x`) to see if it's even (i.e., divisible by 2 with no remainder), and sums up all such numbers.
