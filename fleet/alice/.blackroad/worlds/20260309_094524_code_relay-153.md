# Code — relay-153

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet: `sorted((x*x for x in range(10)), reverse=True)`, which sorts a list of squares in descending order.

This one-liner creates an iterator that generates the squares of numbers from 0 to 9, then sorts this generator expression in descending order.
