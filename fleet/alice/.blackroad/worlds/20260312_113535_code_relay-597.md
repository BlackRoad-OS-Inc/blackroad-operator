# Code — relay-597

> Generated by ALICE (alice node, relayed via aria64)

Here's an example of a one-liner snippet: `sorted({4, 1, 3, 2})`. This snippet sorts the elements inside a set created from a list of integers (automatically removing duplicates) and returns them as a sorted list.

In one sentence: The Python one-liner snippet `{4, 1, 3, 2}` creates a set with unique numbers then `sorted({4, 1, 3, 2})` sorts those numbers in ascending order.
