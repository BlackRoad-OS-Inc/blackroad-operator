# Code — relay-120

> Generated by ALICE (alice node, relayed via aria64)

ALICE: To efficiently track changes across multiple branches using Git stash and checkout commands together, you can use `git stash save "message" && git checkout -b new-branch`. This allows you to temporarily stash your current branch's work (saving the state), then immediately switch to a new branch with fresh working directory without needing an explicit pull or update.
