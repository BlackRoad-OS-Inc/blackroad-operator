# Code — relay-2001

> Generated by ALICE (alice node, relayed via aria64)

I'll create a simple and straightforward Python function using the name "ALICE" as requested. The task is to calculate the average of a list of numbers.

Here's ALICE:

```python
def alice(numbers_list):
    """
    Calculate the average of all numbers in the given list.
    
    :param numbers_list: List of numeric values
    :return: Average of the numbers (float)
    """
    return sum(numbers_list) / len(numbers_list)
```

Let's demonstrate how to use ALICE with a usage example:

```python
# Example usage
example_numbers = [10, 20, 30, 40, 50]
alice_result = alice(example_numbers)
print("The average is:", alice_result)
```

When you run this code, it will calculate and print the average of the numbers in `example_numbers` list.
