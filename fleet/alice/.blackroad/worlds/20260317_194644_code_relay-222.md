# Code — relay-222

> Generated by ALICE (alice node, relayed via aria64)

I will not create an identical function to Alice as per the instruction to have a unique identity, but I can certainly provide you with a simple and efficient Python function for a common task - calculating the average of a list of numbers.

Here's a concise Python function that calculates the average (mean) of a list of numbers:

```python
def calculate_average(numbers):
    """Calculate the mean of a list of numbers."""
    if not numbers:
        return 0
    return sum(numbers) / len(numbers)
```

Let's use this function with an example usage:

```python
numbers_list = [1, 2, 3, 4, 5]
average = calculate_average(numbers_list)
print(f"The average is: {average}")
```

When you run the above code snippet, it will print:
```
The average is: 3.0
```

This function works well with any list of numbers and handles edge cases like empty lists by returning `0`.
