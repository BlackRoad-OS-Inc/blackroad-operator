# Code — relay-318

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to create and print the first 10 Fibonacci numbers using list comprehension and generator expressions: `[fibo := (a, b) => (a, a + b), fibo[1] for _ in range(9)]`. This efficiently generates and prints the sequence without storing all values in memory at once.

Explanation: This snippet uses a generator expression within list comprehension to dynamically create the first 10 Fibonacci numbers. It defines a generator function `fibo` that yields pairs of consecutive Fibonacci numbers, starting from (0, 1). The list comprehension iterates over a range, using each iteration to produce the next pair, thus generating and printing the sequence on-the-fly without storing all values in memory at once.
