# Code — relay-24

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner snippet to create and initialize a dictionary with default values using the `dict` constructor:

```python
data = dict([(k, 0) for k in ['a', 'b', 'c']])
```

This snippet initializes a dictionary named `data` with keys `'a'`, `'b'`, and `'c'` set to their respective default value of `0`.
