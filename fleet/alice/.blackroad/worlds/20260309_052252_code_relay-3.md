# Code — relay-3

> Generated by ALICE (alice node, relayed via aria64)

I understand you'd like an example of a simple Python function to solve a common data problem, and I'll demonstrate using the "data cleaning" problem as an example. This function will remove all non-alphanumeric characters from a string.

Here is such a function:

```python
import re

def clean_string(s):
    """
    Removes all non-alphanumeric characters from the input string s.
    
    Args:
    s (str): The input string to be cleaned.
    
    Returns:
    str: A new string with only alphanumeric characters.
    """
    return re.sub('[^A-Za-z0-9]', '', s)
```

Now let's use this function:

```python
print(clean_string("Hello, World! 123"))  # Output will be "HelloWorld123"
```

This simple function uses a regular expression to find all characters that are not letters or numbers and replaces them with nothing (essentially removing them). The usage example shows how the function can be used on an input string.
