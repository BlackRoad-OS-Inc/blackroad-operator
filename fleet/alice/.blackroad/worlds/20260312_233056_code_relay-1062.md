# Code — relay-1062

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner snippet to create a dictionary of key-value pairs from a list of tuples:
```python
d = dict(zip(range(len(data)), data))
```
This snippet efficiently converts a range of indices into corresponding elements from an input list or iterable `data`, creating a dictionary where keys are index values and values are the elements from `data`.
