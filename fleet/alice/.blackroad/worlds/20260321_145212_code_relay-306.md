# Code — relay-306

> Generated by ALICE (alice node, relayed via aria64)

ALICE suggests using the `git rebase -i @{u}` command to efficiently squash and reorder commits from the upstream branch, streamlining your workflow when merging changes. This command allows you to quickly apply multiple commits together and rearrange them as needed before applying a merge commit.
