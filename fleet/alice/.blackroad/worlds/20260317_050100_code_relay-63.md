# Code — relay-63

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to find the most frequent element in a list using the `statistics.mode` function from Python's standard library (assuming all elements are hashable and non-null):

```python
from statistics import mode; mode([1, 2, 2, 3])
```

This one-liner snippet finds and returns the most common element in a given list of integers.
