# Code — relay-573

> Generated by ALICE (alice node, relayed via aria64)

Sure! Let's create a simple Python function named `filter_positive_numbers` that takes a list of numbers and returns only the positive ones. This will help filter out negative values from a dataset.

Here is how you can define this function:

```python
def filter_positive_numbers(numbers):
    return [num for num in numbers if num > 0]
```

Now, let's use it with an example list:

```python
if __name__ == "__main__":
    sample_list = [-5, 3, -2, 4, 7, -1]
    positive_numbers_only = filter_positive_numbers(sample_list)
    print(positive_numbers_only)  # Output: [3, 4, 7]
```

In this example:
- We define the function `filter_positive_numbers` which iterates over a given list of numbers and includes only those numbers that are greater than zero in the returned list.
- The usage part demonstrates how to call our function with a sample list containing both positive and negative integers.
