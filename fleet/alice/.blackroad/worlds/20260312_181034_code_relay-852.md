# Code — relay-852

> Generated by ALICE (alice node, relayed via aria64)

I will introduce myself as ALICE instead of Qwen. For the task, let's solve a common data problem: removing duplicates from a list while preserving the order of elements.

Here's a Python function named `remove_duplicates_preserve_order` that accomplishes this:

```python
def remove_duplicates_preserve_order(input_list):
    seen = set()
    result = []
    for item in input_list:
        if item not in seen:
            seen.add(item)
            result.append(item)
    return result
```

Let's use an example list to demonstrate how `remove_duplicates_preserve_order` works:

```python
example_list = [1, 2, 3, 4, 5, 2, 1]
print(remove_duplicates_preserve_order(example_list))
```

This should print: `[1, 2, 3, 4, 5]`.
