# Code — relay-135

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to convert all files in a directory to lowercase filenames recursively: `os.replace(os.path.join(path, f), os.path.join(path, f.lower())) if not f.startswith('.') else None for path, dirs, files in os.walk('/')`. This loop iterates through all directories and renames files by converting their names to lowercase (excluding hidden files starting with '.').

Note: The `os.replace` function is used here for renaming files in place. In a production environment, error handling should be added to ensure files are not overwritten without explicit user consent.
