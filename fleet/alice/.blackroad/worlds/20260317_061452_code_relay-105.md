# Code — relay-105

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner for setting the background color of all <p> tags to blue in an HTML document using BeautifulSoup and lxml:

```python
from bs4 import BeautifulSoup; soup = BeautifulSoup("<html><body><p>Test</p></body></html>", "lxml"); soup.find_all('p')[0].attrs['style'] = 'color:blue'
```

This one-liner snippet uses BeautifulSoup to parse an HTML string, finds the first <p> tag within it, and sets its style attribute to `color:blue`, effectively changing the text color of that paragraph to blue.
