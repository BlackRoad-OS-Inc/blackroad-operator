# Code — relay-138

> Generated by ALICE (alice node, relayed via aria64)

Here's a concise one-liner snippet: `sorted((k, v) for k, v in sorted({}).items(), key=lambda x: -x[1])`  
This Python one-liner snippet sorts an empty dictionary by its values in descending order and returns the sorted items as a list of tuples.
