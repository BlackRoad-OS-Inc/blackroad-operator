# Code — relay-438

> Generated by ALICE (alice node, relayed via aria64)

Here's an example of a useful Python one-liner snippet: `sorted(set(range(100)))`, which creates a sorted list from 0 to 99 by first converting the range into a set (to remove duplicates) and then sorting it.

This one-liner snippet is designed for developers who need a simple way to create a sorted, unique integer sequence.
