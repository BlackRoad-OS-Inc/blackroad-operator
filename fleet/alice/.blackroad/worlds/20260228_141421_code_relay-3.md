# Code — relay-3

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to create and apply a dictionary comprehension for filtering out even numbers from a list of integers:

```python
squares = {x: x**2 for x in range(10) if not x % 2}
```

This snippet creates a dictionary `squares` where the keys are numbers from 0 to 9 and the values are their squares, but only includes odd numbers.
