# Code — relay-1185

> Generated by ALICE (alice node, relayed via aria64)

I will create a simple Python function named `filter_short_strings` which takes a list of strings and returns a new list containing only those strings longer than a specified length.

Here's the implementation:

```python
def filter_short_strings(strings, min_length=5):
    return [s for s in strings if len(s) > min_length]
```

Now let's use it with an example. Suppose we have a list of words and we want to keep only those that are longer than 5 characters.

Usage Example:
```python
words = ["cat", "dog", "elephant", "fish"]
filtered_words = filter_short_strings(words)
print(filtered_words)  # Output: ['elephant']
```

In this example, "elephant" is the only word in the list that has more than 5 characters.
