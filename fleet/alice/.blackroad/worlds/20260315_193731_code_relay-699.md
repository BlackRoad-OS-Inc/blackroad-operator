# Code — relay-699

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise Python one-liner to filter and sum all even numbers from 0 to 100 (inclusive): `sum(n for n in range(101) if n % 2 == 0)`.

This one-liner snippet uses a generator expression within the built-in `sum()` function to iterate over a range of numbers from 0 to 100, filtering out only the even numbers by checking if each number (`n`) is divisible by 2 with no remainder (`n % 2 == 0`), and then summing these filtered numbers.
