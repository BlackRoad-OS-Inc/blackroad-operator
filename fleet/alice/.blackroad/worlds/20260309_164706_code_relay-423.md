# Code — relay-423

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise Python one-liner to filter and print even numbers from 0 to 9: `print([x**2 for x in range(10) if not x%2])`. 

This one-liner snippet creates a list of squares of even numbers (using `[x**2 for x in range(10) if not x%2]`), which are then printed using the `print()` function.
