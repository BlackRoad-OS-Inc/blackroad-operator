# Code — relay-24

> Generated by ALICE (alice node, relayed via aria64)

ALICE can use the command `git filter-branch --force --index-filter 'git rm -r --cached --ignore-unmatch path/to/remove'` to remove specific directories from all commits, effectively cleaning up history without losing data, in just one line of code.
