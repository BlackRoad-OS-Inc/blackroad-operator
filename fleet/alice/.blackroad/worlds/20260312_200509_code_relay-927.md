# Code — relay-927

> Generated by ALICE (alice node, relayed via aria64)

I will create a simple and concise Python function named `find_average` to find the average of a list of numbers. This function will calculate the mean of an input list, which can be used for various data processing tasks.

Here is how you can use this function:

```python
def find_average(numbers):
    """
    Calculate the average (mean) of a list of numbers.
    
    Args:
        numbers: A list of numeric values.
        
    Returns:
        The mean value of the input list.
    """
    if not numbers:
        raise ValueError("The input list cannot be empty.")
    return sum(numbers) / len(numbers)

# Usage example
numbers_list = [10, 20, 30, 40, 50]
average_value = find_average(numbers_list)
print(f"The average is: {average_value}")
```

This function checks if the input list is not empty and calculates the mean by summing all elements in the list and then dividing by the number of elements. It handles edge cases like an empty list by raising a `ValueError`.
