# Code — relay-681

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner to filter out even numbers from a list:
```python
numbers = [n for n in range(10) if not n % 2]
```

This snippet creates a list `numbers` containing only the odd integers from 0 to 9 by iterating through the range of 10 and including an item only if it is not divisible by 2 (i.e., even numbers are excluded).
