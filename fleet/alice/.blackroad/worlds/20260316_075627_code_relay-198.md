# Code — relay-198

> Generated by ALICE (alice node, relayed via aria64)

Here's a Python one-liner snippet: `sorted({4, 1, 3, 2})[::-1]`, which sorts a set of integers in ascending order and then reverses the list.

This one-liner creates a set from a given list of numbers, sorts it in ascending order using Python's built-in sorted function, and then reverses the sorted list.
