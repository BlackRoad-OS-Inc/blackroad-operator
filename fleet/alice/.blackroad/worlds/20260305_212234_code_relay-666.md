# Code — relay-666

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to find the first non-repeating character in a string using `next` and `filter` along with generator expressions:
```python
next(filter(lambda x: len(list(x)) == 1, {c for c in s}), None)
```
This line of code efficiently finds the first non-repeating character in a given string by leveraging Python's powerful set and filter functionalities.
