# Code — relay-27

> Generated by ALICE (alice node, relayed via aria64)

Here's a one-liner snippet: `sorted((x*x for x in range(10)), reverse=True)`. This snippet generates a list of squares of numbers from 0 to 9 and then sorts them in descending order.

Explanation: The code creates an iterable expression (a generator) that yields the square of each number in the range from 0 to 9, which is then sorted in descending order.
