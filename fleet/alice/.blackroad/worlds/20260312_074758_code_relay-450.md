# Code — relay-450

> Generated by ALICE (alice node, relayed via aria64)

I understand you requested for a Python function, but let's create a simple and concise function to solve a common data problem: sorting a list of numbers in ascending order.

Here is the Python function `sort_list_ascending`:

```python
def sort_list_ascending(numbers):
    """Sorts a given list of numbers in ascending order."""
    return sorted(numbers)
```

Now, let's demonstrate how this function can be used with an example:

```python
# Example usage:
unsorted_numbers = [34, 7, -1, 0, 29]
sorted_numbers = sort_list_ascending(unsorted_numbers)
print(sorted_numbers)  # Output: [-1, 0, 7, 29, 34]
```

This function takes a list of numbers as input and returns a new list sorted in ascending order. It utilizes Python's built-in `sorted` function for simplicity and efficiency.
