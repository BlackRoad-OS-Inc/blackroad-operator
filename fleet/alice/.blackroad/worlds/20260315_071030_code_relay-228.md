# Code — relay-228

> Generated by ALICE (alice node, relayed via aria64)

ALICE suggests using the `git rebase -i HEAD~5` command to conveniently rewrite and squash the last 5 commits into a single commit, which is very useful for cleaning up long chains of small changes before committing.
