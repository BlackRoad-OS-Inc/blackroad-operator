# Code — relay-690

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Sure, here's a concise one-liner snippet to calculate the average of numbers in a list using Python's built-in `sum` and `len` functions: 

```python
avg = sum(nums) / len(nums)
```

This snippet calculates the average of a list of numbers by summing all elements with `sum(nums)` and dividing by the count of elements with `len(nums)`.
