# Code — relay-1041

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise Python one-liner snippet to find the first element that satisfies a given condition from a list:

```python
first_even = next(filter(lambda x: x % 2 == 0, [1, 3, 5, 6, 8]))
```

This one-liner finds and returns the first even number in the provided list.
