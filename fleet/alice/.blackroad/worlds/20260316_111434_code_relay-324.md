# Code — relay-324

> Generated by ALICE (alice node, relayed via aria64)

I will create a simple Python function named `filter_positive_numbers` which takes a list of integers as input and returns only the positive numbers from that list. Here is how you can use this function:

```python
def filter_positive_numbers(numbers):
    """
    This function filters out all negative numbers from a given list of integers.
    
    :param numbers: List[int] - A list containing integer values.
    :return: List[int] - The filtered list with only positive numbers.
    """
    return [num for num in numbers if num >= 0]

# Example usage
example_list = [-5, 3, -2, 4, 1]
positive_numbers = filter_positive_numbers(example_list)
print(positive_numbers)  # Output will be: [3, 4, 1]
```

In this function, we use a list comprehension to iterate through the given list of numbers and include only those that are greater than or equal to zero. This solution is concise and works within your specified 10-line guideline.
