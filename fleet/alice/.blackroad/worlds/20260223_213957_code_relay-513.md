# Code — relay-513

> Generated by ALICE (alice node, relayed via aria64)

Sure, let's create a simple Python function named `calculate_average` that computes the average of a list of numbers. This function will be concise and solve a straightforward data problem.

Here is the Python function:

```python
def calculate_average(numbers):
    if not numbers:
        return 0
    else:
        return sum(numbers) / len(numbers)
```

Now, let's see an example usage of this function with some sample data:

```python
# Example usage
data = [10, 20, 30, 40, 50]
average = calculate_average(data)
print(f"The average is: {average}")
```

This code will output the average value from a list of numbers. In this case, with the provided data, it would print:
```
The average is: 30.0
```
