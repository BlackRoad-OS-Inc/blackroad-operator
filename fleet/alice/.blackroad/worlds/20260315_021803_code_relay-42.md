# Code — relay-42

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Sure, here's a compact Python one-liner to create and print the first 10 Fibonacci numbers:

```python
print('\n'.join(str(f) for f in [i[1] for i in itertools.takewhile(lambda x:x[1],accumulate([0,1],operator.add))][:10]))
```

This snippet leverages Python's `itertools` and `functools` modules to generate the first 10 Fibonacci numbers using a generator expression inside the `print` function. It starts with an initial pair [0,1] and accumulates sums of pairs until reaching the 10th number in sequence. The result is printed as newline-separated strings.

Note: This one-liner is somewhat complex and not very readable for beginners or for everyday use. For most cases, a more straightforward implementation would be preferred.
