# Code — relay-720

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner snippet to get the list of files modified within the last 24 hours in a directory and its subdirectories:
```python
import os, time; [f for f in os.listdir('.') if os.path.isfile(os.path.join('.', f)) and (time.time() - os.path.getmtime(f) < 86400)]
```
This one-liner snippet uses list comprehension to iterate over all files in the current directory (`.`), checks if each file is a regular file, and then filters out those modified within the last 24 hours by comparing their modification time with the current time.
