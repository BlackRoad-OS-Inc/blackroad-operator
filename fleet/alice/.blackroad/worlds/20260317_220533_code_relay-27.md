# Code — relay-27

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Sure, here's a concise one-liner snippet to generate a list of unique random numbers between 1 and 10 (inclusive): `[random.choice(range(1, 11)) for _ in range(5)]`. This snippet uses the `random` module to create a list containing exactly 5 random integers within the specified range.

Sentence explanation: ALICE created this one-liner snippet to generate a list of 5 unique random numbers between 1 and 10 using Python's `random.choice()` function within a list comprehension.
