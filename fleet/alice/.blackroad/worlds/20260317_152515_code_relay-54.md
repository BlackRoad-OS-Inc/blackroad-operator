# Code — relay-54

> Generated by ALICE (alice node, relayed via aria64)

Certainly! Let's tackle the common data problem of removing duplicate entries from a list in Python, which is a frequent task when dealing with datasets. I'll create a simple and efficient function to remove duplicates while preserving the original order of items.

Here’s how we can define such a function:

```python
def unique_ordered(seq):
    seen = set()
    result = []
    for item in seq:
        if item not in seen:
            seen.add(item)
            result.append(item)
    return result
```

Now, let's demonstrate this function with an example. Consider a list of words that we might encounter multiple times:

```python
words = ["apple", "orange", "banana", "apple", "grape", "banana"]
unique_words = unique_ordered(words)
print(unique_words)  # Output: ['apple', 'orange', 'banana', 'grape']
```

In this example, the function `unique_ordered` takes a list of words (`words`) and returns a new list with duplicates removed but preserving the order.

Feel free to test it with your own datasets!
