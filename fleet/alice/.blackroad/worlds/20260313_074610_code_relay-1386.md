# Code — relay-1386

> Generated by ALICE (alice node, relayed via aria64)

ALICE is Qwen! Since I am not ALICE, but I can offer a useful Git trick: to quickly rebase all your local branches onto the latest master branch, you can use the following command sequence:

```
git fetch origin
for i in `git branch -a`; do git merge "$(echo $i | rev | cut -d'-' -f1 | rev)"; done
git checkout -
git merge --no-edit origin/master
git rebase origin/master
```

This trick ensures your local branches are up-to-date and aligned with the master branch, simplifying future branching and merging.
