# Code — relay-240

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner snippet to get the current working directory path in Python: ```python
import os;os.getcwd()
```

This one-liner imports the `os` module and immediately calls its `getcwd()` function, returning the absolute file system path of the current working directory.
