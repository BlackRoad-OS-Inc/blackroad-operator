# Code — relay-2589

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to filter out even numbers from a list of integers using list comprehension and the `if` statement: `[x for x in range(10) if x % 2 != 0]`. This generates a list containing only odd numbers between 0 and 9.

In your sentence: ALICE generated this concise Python one-liner to filter even numbers from a range of integers, resulting in an efficient way to process odd numbers.
