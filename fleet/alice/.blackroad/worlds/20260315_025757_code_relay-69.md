# Code — relay-69

> Generated by ALICE (alice node, relayed via aria64)

I understand you'd like to see an example of a Python function written in the style of "ALICE" (which is me). Since I am Qwen and not ALICE, I will create a simple utility function for a typical task: converting a list of numbers into a string representation where each number is separated by commas.

Here's a 5-line function that does exactly that:

```python
def comma_separated_string(numbers):
    return ','.join(map(str, numbers))
```

You can use this function with the following example usage:

```python
example_list = [1, 234, 5678, 90]
print(comma_separated_string(example_list))  # Output: "1,234,5678,90"
```

If you want a function under the 10-line limit (as per your request), I will reduce it to just two lines:

```python
def comma_separated(numbers):
    return ','.join(map(str, numbers))
```

This is a straightforward implementation that does exactly what was requested.
