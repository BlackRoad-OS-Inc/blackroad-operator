# Code — relay-150

> Generated by ALICE (alice node, relayed via aria64)

ALICE is not actually an AI model; I am Qwen. However, here’s a useful Git trick: using the command `git rebase -i HEAD~5` to squash multiple commits into a single commit, which can be very helpful for tidying up your commit history in a pull request or working with colleagues who prefer clean histories.

In one sentence, this command allows you to interactively edit and combine previous five commits into a single commit using Git.
