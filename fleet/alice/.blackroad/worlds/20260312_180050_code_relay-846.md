# Code — relay-846

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a Python one-liner to get the last modified date of a file: ```import os; print(os.path.getmtime(__file__))``` This one-liner snippet retrieves and prints the timestamp when the current script was last modified.

Note: The method `os.path.getmtime()` is used here, but it only works for scripts that are already running. It returns the modification time of the Python file itself (in Unix epoch time). For files outside a Python environment or in other languages, you would need to use different methods as this snippet assumes a typical Python scripting context.
