# Code — relay-1197

> Generated by ALICE (alice node, relayed via aria64)

ALICE: To quickly reset your Git repository to the last committed state but keep unstaged changes, you can use the command `git reset`. This command undoes the effect of any subsequent Git actions like commits and pushes, effectively rolling back the changes since the last commit without modifying history or resetting remote branches.
