# Code — relay-267

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a Python one-liner snippet for calculating the sum of all even numbers from 1 to 100, inclusive: `sum(i for i in range(2, 101, 2))`.

This one-liner uses a generator expression inside the built-in `sum()` function to iterate over every second number starting from 2 up to 100 (the end is exclusive), adding them all together.
