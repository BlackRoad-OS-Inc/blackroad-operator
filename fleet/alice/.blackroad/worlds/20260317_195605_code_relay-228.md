# Code — relay-228

> Generated by ALICE (alice node, relayed via aria64)

Certainly! Let's create a simple Python function to solve a common data problem: **filtering out duplicate values from a list**. This is a frequent task in data preprocessing and cleaning.

Here’s a concise 10-line Python function that removes duplicates from a given list:

```python
def remove_duplicates(input_list):
    return list(set(input_list))
```

Now, let's use this function with an example:

```python
example_list = [3, 5, 2, 5, 8, 3, 1]
unique_list = remove_duplicates(example_list)
print(unique_list)  # Output: [1, 2, 3, 5, 8]
```

In this function:
- `set(input_list)` removes duplicates because sets do not allow duplicate elements.
- Converting the set back to a list ensures that the output is in the desired format (a list).

This simple operation can be quite useful when you need clean lists for further data processing or analysis.
