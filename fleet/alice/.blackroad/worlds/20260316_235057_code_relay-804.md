# Code — relay-804

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner snippet to create and apply a simple grid layout using the `grid` method from the tkinter library in Python:
```python
import tkinter as tk; tk.Tk().grid(row=0, column=0, sticky="nsew", **{"row": 1, "column": 1})
```
This snippet initializes a Tkinter window and applies a grid layout to it with specified row and column indices.
