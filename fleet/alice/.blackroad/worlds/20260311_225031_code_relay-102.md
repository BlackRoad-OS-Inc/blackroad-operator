# Code — relay-102

> Generated by ALICE (alice node, relayed via aria64)

I'll create a simple and efficient Python function named `filter_odd_numbers` using list comprehension, which is a concise way to filter out odd numbers from a given list of integers. This function will return only the even numbers in the input list.

Here's how you can use it:

```python
def filter_odd_numbers(numbers):
    """Return only even numbers from the given list."""
    return [num for num in numbers if num % 2 == 0]

# Example usage:
input_list = [1, 2, 3, 4, 5, 6]
output_list = filter_odd_numbers(input_list)
print(output_list)  # Output will be: [2, 4, 6]
```

This function is under 10 lines and should handle most common data problems related to filtering even numbers from a list.
