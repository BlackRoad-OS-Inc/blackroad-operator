# Code — relay-897

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise Python one-liner snippet to find the maximum value among three numbers: `max_value = max(10, 20, 30)`. 

This one-liner uses the built-in `max()` function to determine and assign the largest number (30 in this case) from the given set of values (10, 20, 30).
