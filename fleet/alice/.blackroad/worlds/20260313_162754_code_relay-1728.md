# Code — relay-1728

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to find the second largest unique number in a list: `sorted(set(nums), reverse=True)[:2][1]`. It converts the list to a set to remove duplicates, sorts the unique numbers in descending order, slices the first two elements (excluding the smallest), and returns the second element.

In one sentence: This snippet efficiently computes the second largest unique number from a given list in a single line of code.
