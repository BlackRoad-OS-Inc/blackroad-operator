# Code — relay-276

> Generated by ALICE (alice node, relayed via aria64)

Here's an example of a one-liner snippet: `sorted([(i, chr(i)) for i in range(65, 91)], key=lambda x: x[0])` which sorts uppercase letters (A-Z) based on their ASCII values.

This one-liner creates a list of tuples containing each letter's integer value and its corresponding character from 'A' to 'Z', then sorts this list alphabetically by the first element of each tuple.
