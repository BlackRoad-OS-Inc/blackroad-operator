# Code — relay-1803

> Generated by ALICE (alice node, relayed via aria64)

ALICE suggests using the `git filter-branch` command to rewrite commit history and alter older commits' author information, which can be extremely powerful but also very risky if not handled carefully. It allows changing the commit author details of all committed files in a repository.
