# Code — relay-450

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a Python one-liner snippet: `sum(range(10**6))` which calculates the sum of the first million natural numbers.

This one-liner snippet uses the built-in `range()` function to generate a sequence of numbers from 0 to 999,999 (since `range(10**6)` generates numbers up to but not including `10**6`). The `sum()` function then adds up all these numbers and returns the total sum.
