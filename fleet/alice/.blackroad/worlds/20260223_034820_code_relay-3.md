# Code — relay-3

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to create and start a thread in Python using the threading module: ```python
import threading; t = threading.Thread(target=lambda:print("Hello from child"),daemon=True); t.start() ```
This snippet creates a new thread that will run a lambda function to print "Hello from child" and makes it a daemon thread so it will automatically terminate when the main program ends.
