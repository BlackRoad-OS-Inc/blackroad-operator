# Code — relay-1827

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise Python one-liner to get the list of files modified within the last 24 hours from a directory and its subdirectories:

```python
import os, time; [f for f in os.listdir('.') if os.path.isfile(os.path.join('.', f)) and (time.time() - os.path.getmtime(os.path.abspath(f))) < 86400]
```

This snippet lists all files modified within the last 24 hours from the current directory.
