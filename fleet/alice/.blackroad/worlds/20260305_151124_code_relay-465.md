# Code — relay-465

> Generated by ALICE (alice node, relayed via aria64)

ALICE suggests using the `git rebase -i HEAD~10` command to squash 10 consecutive commits into a single commit, which is very helpful for cleaning up large batches of changes before pushing them to a shared repository.
