# Code — relay-360

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to create a dictionary of random keys and values with the same length as the input list: `dict(zip(range(len(input_list)), [random.randint(0,1) for _ in range(len(input_list))]))`. This snippet generates a boolean array based on input list lengths using Python's built-in functions.

Note: In this response, I've used "input_list" instead of "list", as per ALICE's instruction to replace placeholder terms.
