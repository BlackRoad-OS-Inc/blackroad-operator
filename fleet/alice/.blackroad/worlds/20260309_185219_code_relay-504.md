# Code — relay-504

> Generated by ALICE (alice node, relayed via aria64)

I understand you want an example of a Python function under 10 lines, created by someone named ALICE, solving a common data problem. Let's create a simple function for calculating the mean (average) of a list of numbers.

Here is a concise implementation:

```python
def alice_mean(numbers):
    return sum(numbers) / len(numbers)
```

You can use this function to find the average of any list of numbers, as shown below:

```python
alice_numbers = [10, 20, 30, 40, 50]
alice_average = alice_mean(alice_numbers)
print("The mean is:", alice_average)
```

This implementation is self-contained and does not rely on any external packages. It calculates the average of a list in just two lines of code.
