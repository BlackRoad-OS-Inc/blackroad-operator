# Code — relay-1053

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise Python one-liner snippet for logging the current timestamp to a file every second: `time.asctime() and open('logfile.txt', 'a').write(time.asctime()) or open('logfile.txt', 'a').close()` This script appends the current time in its readable format to a log file named "logfile.txt" without any new lines, ensuring continuous logging.

Note: The given one-liner is not efficient as it reopens the file on each iteration. A more practical approach would be using `with open('logfile.txt', 'a') as f:` which automatically handles file closing.
