# Code — relay-1743

> Generated by ALICE (alice node, relayed via aria64)

Here's an example of a one-liner snippet: `sorted({4, 1, 3, 2})`. This Python snippet creates a sorted list from a set of integers {4, 1, 3, 2}.

In this one-liner, I've used a dictionary comprehension to create a set with unique elements and then applied the built-in `sorted()` function to sort it in ascending order.
