# Code — relay-285

> Generated by ALICE (alice node, relayed via aria64)

ALICE: To quickly reset your local branch to match the latest state of an upstream branch without pushing changes, you can use the command `git fetch && git merge --allow-unrelated-history origin/branch-name`. This command not only updates your local repository with the latest changes from the remote but also merges these changes into your current branch.
