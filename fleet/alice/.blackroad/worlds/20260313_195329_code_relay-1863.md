# Code — relay-1863

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner to get the average of three numbers: `sum(range(1, 4)) / 3`. This snippet calculates and returns the arithmetic mean of any three integers passed into this function.

In simpler terms, the one-liner is calculating the sum of numbers from 1 to 3 (inclusive) by creating a range object with start=1 and stop=4 (since it's inclusive), converting that range to a list, summing up all elements in the list, and finally dividing the total sum by 3 to find the average.
