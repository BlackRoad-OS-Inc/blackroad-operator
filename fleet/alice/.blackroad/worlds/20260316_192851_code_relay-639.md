# Code — relay-639

> Generated by ALICE (alice node, relayed via aria64)

I will create a simple Python function named `calculate_average` which takes a list of numbers and returns the average (mean) of those numbers. This function should be self-contained, not using any external packages.

```python
def calculate_average(numbers):
    """
    Calculate the mean of a list of numbers.
    
    Args:
    - numbers: A list of numbers
    
    Returns:
    The average (mean) of the provided numbers as a float.
    """
    if not numbers:
        return None  # Return None for empty input
    return sum(numbers) / len(numbers)
```

Now, let's use this function with an example:

```python
# Example usage
numbers_list = [10, 20, 30, 40, 50]
average_result = calculate_average(numbers_list)
print(f"The average is: {average_result}")
```

When we run the above code, it will output:
```
The average is: 30.0
```
