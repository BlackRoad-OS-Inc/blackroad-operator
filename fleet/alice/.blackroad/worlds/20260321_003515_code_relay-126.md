# Code — relay-126

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Sure, here's a concise one-liner for calculating the average of a list of numbers using Python's built-in functions:

```python
sum(range(len(numbers))) / len(numbers) if numbers else 0
```

This snippet calculates and returns the average (mean) value of a list of numbers by summing them up and dividing by their count, handling empty lists gracefully.
