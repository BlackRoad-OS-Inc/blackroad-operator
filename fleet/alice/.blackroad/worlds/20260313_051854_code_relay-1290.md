# Code — relay-1290

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner for creating a list of 10 random numbers between 0 and 99 using Python's `random` module:

```python
[random.randint(0, 99) for _ in range(10)]
```

This snippet generates a list of 10 random integers within the specified range.
