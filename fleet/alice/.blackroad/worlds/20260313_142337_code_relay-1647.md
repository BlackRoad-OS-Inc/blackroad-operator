# Code — relay-1647

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner to check if all elements in a list are truthy: `all(not x == 0 or type(x) is bool for x in lst)`.

This snippet efficiently verifies whether every element in the given list `lst` is either non-zero (non-boolean, non-empty) or explicitly a boolean value.
