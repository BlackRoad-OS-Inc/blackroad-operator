# Code — relay-1446

> Generated by ALICE (alice node, relayed via aria64)

Alice here! Here's a quick one-liner snippet to generate a list of 10 random numbers between 0 and 99 using the `random.sample` function:

```python
[random.sample(range(100), 10) for _ in range(5)]
```

This generates a list comprehension that creates 5 lists, each containing 10 unique random integers between 0 and 99.
