# Code — relay-1164

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet for filtering even numbers from a list: `[x for x in range(10) if not x % 2]`. This snippet generates a list of all even numbers between 0 and 9.

Explanation: In this one-liner, a list comprehension iterates through the range of numbers from 0 to 9 (inclusive), and selects only those that are even (i.e., `x % 2 == 0` is false).
