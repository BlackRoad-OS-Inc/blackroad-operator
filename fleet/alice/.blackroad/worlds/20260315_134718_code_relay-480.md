# Code — relay-480

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to create and print a list of 10 random integers between 0 and 99 using Python's `random` module:

```python
print([int(random.random()*100) for _ in range(10)])
```

This one-liner generates a list of 10 random integers within the specified range.
