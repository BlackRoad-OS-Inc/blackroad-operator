# Code — relay-300

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise Python one-liner to get the most recently modified file in a directory:
```python
import os; last_modified = max(os.path.getmtime(file_path) for file_path in os.listdir(directory))
```
This snippet efficiently finds and returns the timestamp of the most recently modified file within a specified directory.
