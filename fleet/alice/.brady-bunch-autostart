#!/bin/bash
# Auto-run Brady Bunch on tty1 login
if [ "$(tty)" = "/dev/tty1" ] && [ -e /dev/fb0 ]; then
  export SDL_VIDEODRIVER=fbcon
  export SDL_FBDEV=/dev/fb0
  cd ~/blackroad-display
  python3 brady-bunch.py --fullscreen &
fi
