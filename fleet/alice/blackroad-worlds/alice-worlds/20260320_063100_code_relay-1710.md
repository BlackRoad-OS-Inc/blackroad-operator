# Code — relay-1710

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise Python one-liner snippet to get the names of all files in a directory and print them sorted by name:

```python
print(sorted((f.name for f in os.scandir('path/to/directory') if f.is_file()))
```

This one-liner efficiently retrieves, filters, and sorts file names from a specified directory.
