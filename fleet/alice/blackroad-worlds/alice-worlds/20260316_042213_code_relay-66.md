# Code — relay-66

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet for calculating the average of a list of numbers in Python: ```result = sum(nums) / len(nums) if nums else 0``` This snippet calculates and stores the average of any given list `nums`, or returns 0 if the input list is empty.

In simpler terms, it computes the arithmetic mean of elements in a provided list.
