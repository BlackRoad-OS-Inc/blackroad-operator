# Code — relay-702

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to get the last modified time of all files in a directory and sort them by modification time:

```python
sorted((os.path.getmtime(os.path.join(dirpath, filename)), filename) for dirpath, _, filenames in os.walk('.') for filename in filenames)
```

This snippet sorts a list of tuples containing file paths and their corresponding last modified times, making it easy to see the order of changes in files within the current directory.
