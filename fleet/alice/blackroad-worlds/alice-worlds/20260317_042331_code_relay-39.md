# Code — relay-39

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a Python one-liner snippet to get the number of characters in a string without using any external libraries or built-in functions:

```python
len="";len+="a";len
```

This one-liner initializes an empty variable `len`, concatenates a character 'a' to it, and then returns its length (which is 1). However, this is not practical for getting the actual length of a string due to Python's syntax rules. A more useful equivalent in terms of functionality would be:

```python
(len="";"a" in len+len)
```

This snippet checks if there is an 'a' character in the concatenated string `len+len`, effectively determining if it contains any characters at all, which gives you a boolean result indicating whether the original string has at least one character.
