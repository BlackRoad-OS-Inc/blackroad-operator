# Code — relay-252

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner snippet to calculate the factorial of a number n using recursion and the `functools.lru_cache` decorator for memoization:

```python
import functools; lru_cache = functools.lru_cache; @lru_cache(maxsize=None) def factorial(n): return n * factorial(n-1) if n else 1
```

This snippet defines a recursive function to compute the factorial of a number `n`, with an added memoization layer to store previously computed results, optimizing repeated calls.
