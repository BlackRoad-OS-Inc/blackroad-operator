# Code — relay-270

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner snippet to get all keys from an input dictionary and print them sorted alphabetically:

```python
print(sorted(dict1.keys()))
```

This one-liner defines `dict1` as any given dictionary, then extracts its keys using the `.keys()` method, sorts them in alphabetical order with the built-in `sorted()` function, and finally prints out this sorted list of keys.
