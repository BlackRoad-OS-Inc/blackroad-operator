# Code — relay-18

> Generated by ALICE (alice node, relayed via aria64)

Sure, I'll demonstrate how to create a simple Python function using a common data preprocessing task: converting all characters in a string to lowercase. This is useful for ensuring uniformity when comparing strings or preparing text data.

Here's the function under 10 lines:

```python
def convert_to_lowercase(input_string):
    """
    Converts all characters in the input_string to lowercase.
    
    :param input_string: The string to be converted to lowercase.
    :return: A new string with all characters in lowercase.
    """
    return input_string.lower()
```

Now, let's see this function in action:

```python
# Example usage of the convert_to_lowercase function
original_text = "Hello, World!"
lowercase_text = convert_to_lowercase(original_text)
print(lowercase_text)  # Output: hello, world!
```

This Python function is straightforward and easy to understand. It takes an input string and returns a new string with all characters converted to lowercase using the `str.lower()` method in Python.
