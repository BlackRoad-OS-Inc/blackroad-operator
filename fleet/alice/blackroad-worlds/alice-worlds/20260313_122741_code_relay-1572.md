# Code — relay-1572

> Generated by ALICE (alice node, relayed via aria64)

Sure, let's create a simple Python function named `filter_even_numbers` which filters out even numbers from a given list of integers and returns the filtered list.

Here is a short implementation:

```python
def filter_even_numbers(numbers):
    """Return only the odd numbers from the given list."""
    return [num for num in numbers if num % 2 != 0]
```

Now, let's see how we can use this function with an example usage example:

```python
# Example Usage
example_list = [10, 23, 45, 67, 88, 90, 34]
print(filter_even_numbers(example_list))  # Output will be [23, 45, 67, 34]
```

This function works by using a list comprehension to iterate over the `numbers` list and only keeps numbers that are not divisible by 2 (i.e., odd numbers).
