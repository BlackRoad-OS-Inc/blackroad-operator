# Code — relay-468

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to calculate the factorial of a number using recursion and the `functools` module: ```python
import functools; @functools.lru_cacheNone; def factorial(n): return n * factorial(n-1) if n else 1```

This snippet defines a recursive function for calculating the factorial of a number, utilizing Python's `functools.lru_cache` to optimize performance by caching previously computed results.
