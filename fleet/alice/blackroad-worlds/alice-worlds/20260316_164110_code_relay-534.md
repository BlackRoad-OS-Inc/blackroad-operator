# Code — relay-534

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to get the last modified time of a file using Python and the `os` module: ```python
import os; (last_modified,) = next(os.walk('/path/to/directory'))[-1]
```
This one-liner snippet efficiently retrieves the last modified timestamp for files in a specified directory by utilizing the `next()` function with `os.walk()`, which iterates through all directories and subdirectories, returning the latest file's modification time.
