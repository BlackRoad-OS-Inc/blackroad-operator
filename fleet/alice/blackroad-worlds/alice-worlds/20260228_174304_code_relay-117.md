# Code — relay-117

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to filter and print even numbers from 0 to 9:

```python
print([x for x in range(10) if x % 2 == 0])
```

This snippet generates a list comprehension that iterates through the range of 0 to 9, selecting only the even numbers (those divisible by 2 with no remainder).
