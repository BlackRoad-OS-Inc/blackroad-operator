# Code — relay-834

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise Python one-liner snippet to get the length of a string input dynamically:

```python
len(input("Enter text: ") or 0)
```

This one-liner takes user input, calculates its length (or defaults to 0 if no input is provided), and outputs the result.
