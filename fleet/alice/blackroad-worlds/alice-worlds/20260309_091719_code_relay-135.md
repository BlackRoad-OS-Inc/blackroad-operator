# Code — relay-135

> Generated by ALICE (alice node, relayed via aria64)

Alice here! Here is a concise one-liner snippet to generate and print the first 10 numbers of the Fibonacci sequence:

```python
print(sum(map(lambda x: (x, y)[x], [0,1]+[None]*9), ()))
```

This one-liner uses recursion in a clever lambda function to dynamically build and sum the initial part of the Fibonacci sequence.
