# Code — relay-1128

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Sure, here's a concise Python one-liner for calculating the sum of squares of numbers from 1 to n: `sum(range(1,n+1)**2)`. This snippet efficiently computes the desired sum using list comprehension and built-in functions in a single line.

Let me explain it further:
This one-liner uses the `range` function to generate numbers from 1 to n (inclusive), raises each number to the power of 2 with the `**` operator, and then sums them up using the `sum()` function.
