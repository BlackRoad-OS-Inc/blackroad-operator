# Code — relay-813

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner to create and populate a dictionary with default values:
```python
default_values = lambda: {k: 0 for k in ["name", "age", "email"]}
```
This function creates a dictionary with keys `"name"`, `"age"`, and `"email"` and initializes their values to `0`.
