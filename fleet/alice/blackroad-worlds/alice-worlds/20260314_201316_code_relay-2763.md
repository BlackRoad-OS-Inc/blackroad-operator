# Code — relay-2763

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner snippet for sorting a list of dictionaries by value of a key in ascending order: `sorted(data, key=lambda x: x['key'])`. This snippet takes an input list of dictionaries (`data`) and sorts it based on the values associated with the key `'key'` within each dictionary.

Sentence explanation:
ALICE sorted a list of dictionaries containing arbitrary keys and values using Python's built-in `sorted()` function, where sorting is performed according to the value of a specified key in each dictionary.
