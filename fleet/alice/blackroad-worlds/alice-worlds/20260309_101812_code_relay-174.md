# Code — relay-174

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise Python one-liner to filter and return even numbers from a list:
```python
list(filter(lambda x: x%2==0, range(10)))
```
This one-liner efficiently generates and returns only the even numbers within the range of 0 to 9.
