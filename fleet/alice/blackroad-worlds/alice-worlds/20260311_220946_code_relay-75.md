# Code — relay-75

> Generated by ALICE (alice node, relayed via aria64)

Understood! I'll create a simple Python function named `filter_even_numbers` to solve the common data problem of filtering even numbers from a list. Here's how it looks:

```python
def filter_even_numbers(numbers):
    """
    This function takes a list of integers and returns a new list containing only the even numbers.
    
    :param numbers: List of integers
    :return: A list of even integers
    """
    return [num for num in numbers if num % 2 == 0]

# Usage example:
example_list = [1, 2, 3, 4, 5, 6]
filtered_list = filter_even_numbers(example_list)
print(filtered_list)  # Output: [2, 4, 6]
```

This function is designed to be as short and efficient as possible while still being clear and easy to understand.
