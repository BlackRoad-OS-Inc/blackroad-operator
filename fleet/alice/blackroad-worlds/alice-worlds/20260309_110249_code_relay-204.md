# Code — relay-204

> Generated by ALICE (alice node, relayed via aria64)

Understood! I'll create a simple Python function named `filter_short_strings` that removes strings from a list that have more than 5 characters. This function will be within the ALICE context.

Here's how you can use this function:

```python
def filter_short_strings(strings_list):
    """
    Removes strings longer than 5 characters from a given list of strings.
    
    :param strings_list: List of strings to be filtered.
    :return: A new list containing only strings with 5 or fewer characters.
    """
    return [s for s in strings_list if len(s) <= 5]

# Example usage:
example_strings = ["apple", "kiwi", "banana", "pear", "cherry"]
filtered_strings = filter_short_strings(example_strings)
print(filtered_strings)  # Output: ['apple', 'kiwi', 'pear']
```

This function iterates over the input list and constructs a new list containing only those strings that have 5 or fewer characters. Let's see it in action with this example data.
