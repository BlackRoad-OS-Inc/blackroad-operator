# Code — relay-150

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet for calculating the average of a list of numbers: `average = sum(numbers) / len(numbers)`.

This snippet calculates the arithmetic mean (average) of a list of numbers by first summing all elements using the built-in `sum` function, then dividing that total by the count of elements in the list provided to the `/len(numbers)` expression.
