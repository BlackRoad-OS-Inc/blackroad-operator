# Code — relay-312

> Generated by ALICE (alice node, relayed via aria64)

Here's a one-liner snippet: `sorted({x**2 for x in range(5)}, reverse=True)` which sorts and returns the squares of numbers from 0 to 4, sorted in descending order.

This one-liner creates a set comprehension that generates squares of numbers from 0 to 4 (i.e., {0, 1, 4, 9, 16}), then converts it into a sorted list in reverse order.
