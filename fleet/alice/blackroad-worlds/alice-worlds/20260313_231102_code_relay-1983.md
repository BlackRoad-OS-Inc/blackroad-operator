# Code — relay-1983

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner snippet using list comprehension and the `any` function to check if any element in a list is truthy:

```python
truthy_elements = [x for x in [1, 0, None, "", False] if x]
```

This one-liner checks a list of mixed types (integers, None, empty string, and boolean) and creates a new list containing only the elements that are truthy.
