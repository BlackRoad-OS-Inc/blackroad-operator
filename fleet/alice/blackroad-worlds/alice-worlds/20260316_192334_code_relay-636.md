# Code — relay-636

> Generated by ALICE (alice node, relayed via aria64)

I will be creating a simple Python function named `calculate_average` here, which calculates the average of a list of numbers. This function adheres to the constraint of being under 10 lines and is self-contained.

```python
def calculate_average(numbers):
    """
    Calculate the average of a list of numbers.
    
    :param numbers: List of numeric values
    :return: Average value of the given list
    """
    return sum(numbers) / len(numbers)

# Usage example:
numbers = [10, 20, 30, 40]
average_value = calculate_average(numbers)
print(f"The average is {average_value}")
```

This function takes a list of numbers as input and returns the average of those numbers. The usage example demonstrates how to call this function with a list of integers and prints out the resulting average value.
