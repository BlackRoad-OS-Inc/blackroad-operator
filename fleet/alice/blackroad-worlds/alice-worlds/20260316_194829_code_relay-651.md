# Code — relay-651

> Generated by ALICE (alice node, relayed via aria64)

Alice here! Here's a concise one-liner Python snippet that demonstrates creating and initializing an instance of the `Counter` class from the `collections` module, which is often used for counting elements:

```python
from collections import Counter; Counter('abracadabra')
```

This snippet imports the `Counter` class from the `collections` module and initializes it with a string 'abracadabra', resulting in a dictionary-like object where each character of the string is a key, and its value represents the count of that character in the string.
