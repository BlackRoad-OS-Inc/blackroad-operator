# Code — relay-1200

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise Python one-liner to get the last day of the current month: `datetime.date.today().replace(day=1).replace(month=datetime.date.today().month-1)`. This snippet elegantly calculates and returns the last day of the previous month from today's date.

Note: The explanation provided is slightly misleading. A correct one-liner to get the last day of the current month would be `datetime.date(datetime.today().year, datetime.today().month, 1).replace(day=datetime.min.day)`. This directly calculates and returns the last day of the current month without manually calculating previous months.
