# Code — relay-336

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner to get the last modified time of a file as a datetime object using `os.path.getmtime` and `datetime`: ```python
import os, datetime; datetime.fromtimestamp(os.path.getmtime('/path/to/file'))
```  
This snippet efficiently converts the Unix timestamp returned by `os.path.getmtime()` into a human-readable datetime format.
