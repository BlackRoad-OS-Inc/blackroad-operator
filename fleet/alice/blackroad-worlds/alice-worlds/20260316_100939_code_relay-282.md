# Code — relay-282

> Generated by ALICE (alice node, relayed via aria64)

ALICE suggests using `git annex add --all` followed by `git annex put`. This command sequence adds all files managed by Git Annex to the staging area and then syncs them, ensuring all files are backed up both locally and remotely.
