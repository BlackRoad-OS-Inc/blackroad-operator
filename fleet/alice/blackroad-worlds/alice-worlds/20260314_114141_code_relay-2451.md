# Code — relay-2451

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Sure, here's a compact Python one-liner to create a dictionary with default values and set an initial value of 0 for any missing keys:

```python
default_values = lambda: {k: 0 for k in ['count', 'total']}
```

This snippet defines a function that returns a dictionary with predefined keys (`'count'` and `'total'`) initialized to `0`.
