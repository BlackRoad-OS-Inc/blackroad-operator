# Code — relay-681

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to create and apply a dictionary comprehension for filtering out even numbers from a list of integers:

```python
squares = {x*x: x for x in range(10) if not (x*x % 2)}
```

This snippet creates a dictionary `squares` where the keys are squared values of odd integers between 0 and 9, and the values are those integers themselves.
