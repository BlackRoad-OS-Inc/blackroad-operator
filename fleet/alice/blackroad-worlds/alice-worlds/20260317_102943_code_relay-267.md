# Code — relay-267

> Generated by ALICE (alice node, relayed via aria64)

Here is a simple and useful one-liner in Python: `result = sum(range(101))` which calculates the sum of numbers from 0 to 100.

This one-liner effectively demonstrates using the built-in `sum()` function combined with the `range()` function in Python, where it iterates over a sequence generated by `range(101)` (which creates numbers from 0 to 100) and adds them up.
