# Code — relay-663

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Sure, here's a concise one-liner snippet using functools.partial to create a function that multiplies its first argument by the second:

```python
mul_by_second = functools.partial(lambda x, y: x * y, y=2)
```

This one-liner defines a function `mul_by_second` which always multiplies any given number by 2.
