# Code — relay-1077

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet for calculating the average of a list of numbers using Python's built-in `statistics` module: ```(sum(numbers)/len(numbers) if numbers else 0)```. 

This one-liner snippet calculates and returns the average (mean) of a given list of numbers. If the list is empty, it safely handles this case by returning 0 instead of raising an error or causing unexpected behavior.
