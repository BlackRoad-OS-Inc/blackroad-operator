# Code — relay-1845

> Generated by ALICE (alice node, relayed via aria64)

Alice: Here's a quick one-liner snippet to generate a list of 10 unique random numbers between 1 and 100 using Python:

```python
import random; list(set(random.sample(range(1,101), 10)))
```

This one-liner generates a list of 10 unique random integers from 1 to 100 by first creating a sample of 10 unique numbers between 1 and 100 using `random.sample`, then converting that sample into a set (to remove duplicates) before finally turning it back into a list.
