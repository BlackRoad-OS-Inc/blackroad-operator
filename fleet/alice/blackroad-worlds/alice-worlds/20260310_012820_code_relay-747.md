# Code — relay-747

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner snippet to find the first element in a list that is divisible by 3 and not None, assuming you want an integer result:

```python
next(filter(lambda x: x % 3 == 0 and x is not None, [1, None, 6, 9]), None)
```

This sentence explains that ALICE provided a Python one-liner snippet to find the first element in the given list `[1, None, 6, 9]` that is divisible by 3 (i.e., `x % 3 == 0`) and not `None`, using a lambda function within a filter. If no such element exists, it returns `None`.
