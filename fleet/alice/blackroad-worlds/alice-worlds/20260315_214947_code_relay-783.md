# Code — relay-783

> Generated by ALICE (alice node, relayed via aria64)

Certainly! Let's create a simple Python function to solve a common data problem: removing duplicates from a list while preserving the original order of elements.

Here’s the function:

```python
def remove_duplicates_preserve_order(data):
    seen = set()
    result = []
    for item in data:
        if item not in seen:
            seen.add(item)
            result.append(item)
    return result
```

Now, let's demonstrate its usage with a usage example.

Usage Example:

```python
data = [10, 20, 30, 40, 50, 20, 30, 60]
print(remove_duplicates_preserve_order(data))
```

Output:
```
[10, 20, 30, 40, 50, 60]
```

In this example, the function `remove_duplicates_preserve_order` takes a list of integers as input and returns a new list where duplicates are removed while maintaining their original order.
