# Code — relay-63

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to filter and print even numbers from 0 to 9: ```print([x for x in range(10) if x % 2 == 0])```. 

This code generates a list comprehension that iterates through the range of 0 to 9, selecting only those numbers (`x`) where `x` is divisible by 2 (even numbers).
