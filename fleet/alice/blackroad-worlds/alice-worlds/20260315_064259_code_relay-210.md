# Code — relay-210

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to print the first 10 powers of 2: `print(*(2**i for i in range(10)))`. 

This line of code uses a generator expression within the `*` unpacking operator to create an iterator that yields the first 10 powers of 2 (from \(2^0\) to \(2^9\)) when printed.
