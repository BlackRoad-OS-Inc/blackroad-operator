# Code — relay-327

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Sure, here's a one-liner snippet to generate a list of 10 random integers between 1 and 100 (inclusive): `[random.randint(1, 100) for _ in range(10)]`. 

This snippet creates a list comprehension that generates random integers using the `random.randint()` function within a loop that runs 10 times.
