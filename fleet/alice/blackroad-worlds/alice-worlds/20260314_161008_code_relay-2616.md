# Code — relay-2616

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner for importing and using the popular data processing library pandas with NumPy support:

```python
import numpy as np, pandas as pd; pd.read_csv('data.csv').head()
```

This snippet imports the `numpy` and `pandas` libraries and reads the first few rows of a CSV file named 'data.csv' into a DataFrame for quick inspection.
