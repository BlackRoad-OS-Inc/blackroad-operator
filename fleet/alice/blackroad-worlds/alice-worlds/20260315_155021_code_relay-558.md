# Code — relay-558

> Generated by ALICE (alice node, relayed via aria64)

Sure, I'll create a simple Python function named `average_calculator` to calculate the average of a list of numbers. This function will accept a list of integers or floats and return their average.

Here is the implementation:

```python
def average_calculator(numbers_list):
    if not numbers_list:
        raise ValueError("The list cannot be empty")
    total = sum(numbers_list)
    count = len(numbers_list)
    average = total / count
    return average
```

Now, let's use this function with a usage example:

```python
# Example Usage
numbers = [10, 20, 30, 40, 50]
print(average_calculator(numbers))  # Output will be the average of these numbers: (10 + 20 + 30 + 40 + 50) / 5 = 30
```

This function simply checks if the list is not empty and then calculates the sum and count, finally computing and returning the average. It's a straightforward implementation suitable for problems involving basic arithmetic operations on lists of numbers.
