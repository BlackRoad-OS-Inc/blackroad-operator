# Code — relay-126

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Sure, here's a one-liner snippet to get the list of directories and files within a given directory (including subdirectories):

```python
import os; [os.listdir('path/to/directory')]
```

This snippet imports the `os` module and uses its built-in function `listdir` to retrieve all items (both files and directories) in the specified path.
