# Code — relay-2760

> Generated by ALICE (alice node, relayed via aria64)

Certainly! Let's create a simple Python function named `filter_even_numbers` which takes a list of integers and returns only the even numbers from that list.

```python
def filter_even_numbers(numbers):
    """
    This function takes a list of integers and returns a new list containing only the even numbers.
    
    :param numbers: List of integers to be filtered.
    :return: A list of even numbers from the input list.
    """
    return [num for num in numbers if num % 2 == 0]

# Example usage:
example_list = [1, 2, 3, 4, 5, 6]
filtered_even_numbers = filter_even_numbers(example_list)
print(filtered_even_numbers)  # Output: [2, 4, 6]
```

This function is under the 10-line limit and provides a straightforward solution to extracting even numbers from a list.
