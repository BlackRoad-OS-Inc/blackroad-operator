# Code — relay-2670

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to calculate the sum of all even numbers up to and including a given number n using list comprehension and the built-in `sum` function: 

```python
sum(n // 2 + (n % 2))
```

This snippet calculates whether a number is even or odd by checking if it's divisible by 2, then sums these values from 0 to n inclusively. Note that this approach is inefficient for large numbers as `sum` function recalculates the sum multiple times. For better performance, consider using a loop instead.
