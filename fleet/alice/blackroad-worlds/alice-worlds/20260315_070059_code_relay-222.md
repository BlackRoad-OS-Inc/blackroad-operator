# Code — relay-222

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner snippet using the `sum` and list comprehension to calculate the sum of all even numbers between 1 and 100 (inclusive): `sum(i for i in range(2, 101, 2))`. This efficiently computes the desired sum in Python.

Explanation: The one-liner uses a generator expression within the `sum` function to iterate through every second number starting from 2 up to 100 (both inclusive), adding them together.
