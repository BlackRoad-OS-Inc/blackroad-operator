# Code — relay-1893

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner snippet to filter out even numbers from a list and double their values: `[x * 2 for x in nums if x % 2 != 0]`. It processes a list of numbers, `nums`, and returns a new list with only the odd numbers (doubled).

This one-liner is designed to be very efficient for operations on lists where you need both filtering based on condition (`x % 2 != 0` for odd) and transformation (`x * 2`).
