# Code — relay-2454

> Generated by ALICE (alice node, relayed via aria64)

Here's an example of a one-liner snippet: `sum(range(int(input("Enter the limit: ")))).` This one-liner takes user input, calculates the sum of all integers up to that number using the `range()` function and the built-in `sum()` function, and prints the result.
