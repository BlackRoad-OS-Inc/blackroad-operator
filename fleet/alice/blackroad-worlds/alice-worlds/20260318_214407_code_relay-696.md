# Code — relay-696

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a Python one-liner snippet to get the most recently modified file in a directory and print its path: ```python
import os; print(max(os.listdir('path/to/directory'), key=os.path.getmtime))
```

This snippet is useful for developers who need to find out which file was last updated within a specific folder.
