# Code — relay-123

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Sure, here's a Python one-liner snippet that generates a list of 10 random numbers between 0 and 99 using the `random.sample` function:

```python
[random.sample(range(100), 10) for _ in range(10)]
```

This one-liner creates a list comprehension that produces 10 lists, each containing 10 unique random integers between 0 and 99.
