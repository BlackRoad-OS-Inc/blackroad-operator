# Code — relay-258

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to find the maximum value among three variables and print it: `print(max(a:=3, b:=5, c:=1))`. 

This one-liner uses the walrus operator (`:=`) to assign values simultaneously to multiple variables in a single line. It then finds and prints the maximum of those values.
