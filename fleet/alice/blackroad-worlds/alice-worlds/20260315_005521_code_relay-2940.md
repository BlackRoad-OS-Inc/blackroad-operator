# Code — relay-2940

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner snippet to find the most frequently occurring element in a list without using any external libraries:

```python
from collections import Counter; most_common = lambda data: next(iter(Counter(data).most_common(), [None])[0])
```

This snippet defines a function `most_common` that takes a list and returns the most common element, leveraging Python's `collections.Counter` to count occurrences efficiently.
