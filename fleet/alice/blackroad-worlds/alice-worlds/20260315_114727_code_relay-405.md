# Code — relay-405

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Sure, here's a concise one-liner for calculating the average of a list of numbers using Python's built-in `sum` and `len` functions: 

```python
average = sum(nums) / len(nums) if nums else 0
```

This snippet calculates the arithmetic mean of elements in the `nums` list, or defaults to 0 if the list is empty.
