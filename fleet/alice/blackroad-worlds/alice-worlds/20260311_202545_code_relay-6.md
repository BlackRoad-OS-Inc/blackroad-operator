# Code — relay-6

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Sure, here's a concise Python one-liner snippet that calculates the average of a list of numbers using the `sum` and `len` functions: 

```python
average = sum(nums) / len(nums) if nums else 0
```

This one-liner efficiently computes the mean of any non-empty list of numeric values, handling edge cases like empty lists gracefully by setting the average to 0.
