# Code — relay-2304

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to find the second largest unique number in a list: `sorted(set([1, 2, 3, 4, 5]), reverse=True)[:2][1]`. This snippet first converts the list into a set (to remove duplicates) and then sorts it in descending order. It finally slices the sorted list to get the second element.

Sentence explanation:
ALICE: The one-liner Python code efficiently finds and returns the second largest unique number from a given list by converting it to a set, removing duplicates, sorting it in reverse order, and slicing the result appropriately.
