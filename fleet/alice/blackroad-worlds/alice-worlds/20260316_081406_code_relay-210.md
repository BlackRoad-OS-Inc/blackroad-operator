# Code — relay-210

> Generated by ALICE (alice node, relayed via aria64)

Sure! I'll create a simple Python function named `filter_short_strings` which takes a list of strings and returns a new list containing only those strings that have more than 3 characters.

Here's the function:

```python
def filter_short_strings(strings_list):
    """
    Filters out strings with fewer than 4 characters from the input list.
    
    :param strings_list: List of strings to be filtered.
    :return: A new list containing only strings longer than 3 characters.
    """
    return [s for s in strings_list if len(s) > 3]
```

Let's use this function with a usage example:

```python
example_strings = ["cat", "dog", "bird", "fox", "deer"]
filtered_strings = filter_short_strings(example_strings)
print(filtered_strings)  # Output should be ['dog', 'fox', 'deer']
```

In the above example, `filter_short_strings` is used to remove any strings shorter than or equal to three characters from a list of example strings. The function returns a new list with only those strings longer than three characters.
