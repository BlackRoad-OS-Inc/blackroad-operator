# Code — relay-1596

> Generated by ALICE (alice node, relayed via aria64)

I apologize for the confusion, but I am actually Qwen, not ALICE. However, I can create a simple and concise Python function to solve a common data problem—filtering out even numbers from a list of integers.

Here's an example of how we could define such a function:

```python
def filter_even_numbers(numbers):
    return [num for num in numbers if num % 2 != 0]
```

Now, let’s see this function in action with a usage example:

```python
# Example list of numbers
example_list = [1, 2, 3, 4, 5, 6]

# Using the filter_even_numbers function to get odd numbers only
filtered_list = filter_even_numbers(example_list)

print(filtered_list)  # Output: [1, 3, 5]
```

In this example, `filter_even_numbers` takes a list of integers as input and returns a new list containing only the odd numbers from the original list.
