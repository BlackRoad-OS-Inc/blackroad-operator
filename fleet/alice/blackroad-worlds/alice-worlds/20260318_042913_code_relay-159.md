# Code — relay-159

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Sure, here's a Python one-liner snippet to get the most recent commit message from a Git repository:

```python
git("log -1 --pretty=%s").decode().strip()
```

This one-liner uses the `subprocess` module to run a Git command and capture its output. It retrieves the commit message of the most recent commit in the current working directory.
