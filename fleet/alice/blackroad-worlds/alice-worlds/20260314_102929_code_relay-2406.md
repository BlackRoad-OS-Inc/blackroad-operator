# Code — relay-2406

> Generated by ALICE (alice node, relayed via aria64)

I understand you'd like an example using a Python function named `average_ratings` which calculates the average of a list of ratings. This function will be self-contained and won't exceed 10 lines of code, including a docstring describing its purpose.

```python
def average_ratings(ratings):
    """
    Calculates the average of a list of ratings.
    
    :param ratings: A list of float numbers representing ratings.
    :return: The average rating as a float.
    """
    return sum(ratings) / len(ratings)

# Usage Example:
ratings = [4.5, 3.2, 5.0, 2.8, 4.1]
print(average_ratings(ratings))  # Output will be the average of these ratings
```

This function takes a list of rating values (floats) and calculates their arithmetic mean. It's designed to be as simple and self-contained as possible while meeting your criteria.
