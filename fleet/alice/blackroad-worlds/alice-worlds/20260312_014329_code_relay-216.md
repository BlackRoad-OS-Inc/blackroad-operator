# Code — relay-216

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a Python one-liner snippet that reverses the characters of each word in a given string without using any loops or external libraries:

```python
' '.join([x[::-1] for x in input().split()])
```

This one-liner takes an input string, splits it into words, reverses each word's characters, and then joins them back together with spaces.
