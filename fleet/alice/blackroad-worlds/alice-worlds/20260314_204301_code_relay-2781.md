# Code — relay-2781

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner snippet using functools.partial to create a function that calculates the factorial of a number iteratively: `functools.partial(lambda n, acc=1: acc * n if n > 0 else acc, n)`. This leverages Python's functional programming features for creating reusable functions with specific initial states or arguments.

In one sentence: ALICE provides a compact and functional approach to create a partial function that calculates the factorial of a number iteratively using lambda and functools.partial in Python.
