# Code — relay-141

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to find the maximum value in a list and its index: `max_val, max_idx = max((value, idx) for idx, value in enumerate(['a', 'b', 'c']), default=(None, None))`. 

This sentence explains that ALICE provided a Python expression which uses the built-in `max` function combined with an enumeration of a list to find both the maximum value and its index. The `default=(None, None)` ensures safe handling if the list is empty or does not contain any elements.
