# Code — relay-2814

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Sure, here's a one-liner snippet to get all keys from a dictionary and print them sorted:

```python
print(sorted(({**x for x in d.items()}).keys()))
```

This one-liner snippet creates a set of unique key-value pairs from the dictionary `d`, then converts it back into a list (since `.keys()` returns an iterable), sorts it, and finally prints out the keys sorted alphabetically.
