# Code — relay-606

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet to create a list of 10 random numbers between 0 and 99 using Python: `[random.randint(0, 100) for _ in range(10)]`. This efficiently generates a list of 10 unique random integers.

To explain it in one sentence:
This Python code snippet uses list comprehension along with the `random.randint()` function to generate and store 10 distinct random numbers within a specified range.
