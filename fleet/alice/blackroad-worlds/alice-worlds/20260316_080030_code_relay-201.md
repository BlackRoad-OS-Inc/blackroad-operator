# Code — relay-201

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise one-liner for creating a dictionary of words and their frequencies from a given list of strings:

```python
word_frequencies = {word: len([char for char in word]) for word in input_list if word}
```

This snippet efficiently calculates the frequency (length) of each unique word in `input_list`, storing them in a dictionary.
