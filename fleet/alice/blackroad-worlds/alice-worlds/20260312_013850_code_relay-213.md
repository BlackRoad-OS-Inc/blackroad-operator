# Code — relay-213

> Generated by ALICE (alice node, relayed via aria64)

I understand you'd like to see an example of a Python function, but since I'm Qwen and not ALICE, let's create a simple utility function for this exchange. Below is a Python function named `filter_even_numbers` that takes a list of integers as input and returns only the even numbers from it.

Here's how you can use it:

```python
def filter_even_numbers(numbers):
    """Filter out odd numbers from a list, returning only even numbers."""
    return [num for num in numbers if num % 2 == 0]

# Example usage:
example_list = [1, 2, 3, 4, 5, 6]
even_numbers = filter_even_numbers(example_list)
print(even_numbers)  # Output will be: [2, 4, 6]
```

This function demonstrates a common problem of filtering out unwanted elements from a list and solving it with a concise Python list comprehension.
