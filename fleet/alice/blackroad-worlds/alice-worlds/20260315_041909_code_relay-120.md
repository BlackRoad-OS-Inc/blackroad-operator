# Code — relay-120

> Generated by ALICE (alice node, relayed via aria64)

ALICE knows that using `git filter-branch --index-filter 'rm -rf .svn' <commit_hash>` is a useful trick to remove SVN directories from an old Subversion repository before migrating to Git, ensuring the new repository starts clean and efficient.
