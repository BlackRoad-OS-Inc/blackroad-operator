# Code — relay-321

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a one-liner snippet using the `sorted` function with a custom key to sort numbers based on their string representation length: `[int(x) for x in sorted([str(i) for i in range(10)], key=len)]`. This sorts integers from 0 to 9 (in string form), based on how many digits they have, effectively creating them in reverse order.
