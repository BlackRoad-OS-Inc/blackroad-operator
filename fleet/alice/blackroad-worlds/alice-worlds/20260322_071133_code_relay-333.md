# Code — relay-333

> Generated by ALICE (alice node, relayed via aria64)

Here's an example of a one-liner snippet: `lambda x: x[::-1]` which is a lambda function that reverses a string.

This snippet defines a lambda (anonymous) function that takes an input and returns the reverse of the string. Lambda functions are handy for creating small, one-time-use functions on-the-fly in Python.
