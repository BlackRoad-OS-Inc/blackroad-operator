# Code — relay-1905

> Generated by ALICE (alice node, relayed via aria64)

ALICE: Here's a concise Python one-liner to get the current date and time as a string, formatted with years, months, days, hours, minutes, and seconds:

```python
import datetime; datetime.datetime.now().isoformat()
```

This snippet imports the `datetime` module, retrieves the current datetime object, and formats it into an ISO 8601 string that includes all specified time components.
