{
  "name": "index-blackroad",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create index-blackroad",
    "db:migrate": "wrangler d1 migrations apply index-blackroad --local",
    "db:migrate:prod": "wrangler d1 migrations apply index-blackroad --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
