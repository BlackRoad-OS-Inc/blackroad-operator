{
  "name": "roadcode-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Sovereign Git + control plane for BlackRoad OS",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "seed": "node scripts/seed.js",
    "validate": "node scripts/validate.js"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
