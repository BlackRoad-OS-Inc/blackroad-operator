name: Shellcheck

on:
  push:
  pull_request:

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Run shellcheck
        uses: ludeeus/action-shellcheck@v2
        with:
          severity: warning
