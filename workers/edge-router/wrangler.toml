name = "blackroad-edge-router"
main = "worker.js"
compatibility_date = "2024-12-01"

# Routes — all 19 domains + wildcard subdomains
# Deploy with: wrangler deploy

# Custom domains handled via CF dashboard routes
# Each domain pattern: *.domain.tld/* and domain.tld/*

[vars]
ENVIRONMENT = "production"
VERSION = "2.0.0"
