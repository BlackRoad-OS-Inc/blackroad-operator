{
  "name": "blackroad-ai-gateway",
  "version": "2.0.0",
  "private": true,
  "description": "BlackRoad AI Gateway — OpenAI-compatible API at api.blackroad.io",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 execute blackboard --file=./migrations/0001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
