name = "br-router"
main = "index.ts"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
ENVIRONMENT = "production"

# Routes will be added after migrating existing Worker route bindings
# See: /Users/alexa/Downloads/br-router-wrangler.toml for full route table
